## 2024-05-20 - [Avoid Memory Bottlenecks with DB Aggregations]
**Learning:** [Fetching all records and looping them in python to do calculations uses up massive memory for large datasets.]
**Action:** [Use DB operations such as SUM() COUNT() and date() to do the aggregations within the DB instead of fetching them into python's memory footprint.]
