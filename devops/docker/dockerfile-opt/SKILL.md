---
name: dockerfile-opt
description: Use when dockerfile optimization — merged into docker-devops parent. See ../SKILL.md for money protocol.
domain: devops
tags: [devops, docker, dockerfile]
---

# Dockerfile Opt

This skill has been merged into the parent docker-devops skill.

See the comprehensive merged skill at [../SKILL.md](../SKILL.md) for full documentation including money-making protocol, real YAML/Python examples, and orchestration flow.

Key capabilities moved:
- Multi-stage Dockerfile patterns (Node.js, Python) with non-root user
- Dockerfile audit tool (`dockerfile-optimize.py`) with severity-scored findings
- Image size analysis script (`analyze-image-size.sh`)
- `.dockerignore` generation for minimal build context
- Optimization rules: layer ordering, distroless bases, image size targets


## When to Use
Use this skill when working with dockerfile opt.


## Workflow
See the parent skill for authoritative workflow documentation.
