import requests\nimport json\n\n# Define API endpoint and parameters\nurl = "https://api.polymarket.com/v1/markets"\nparams = {\n    "limit": 100,\n    "offset": 0\n}\n\n# Send GET request to API\nresponse = requests.get(url, params=params)\n\n# Check if response was successful\nif response.status_code == 200:\n    # Parse JSON response\n    data = json.loads(response.text)\n    \n    # Extract market data\n    markets = data["markets"]\n    \n    # Initialize dictionary to store market categories\n    categories = {}\n    \n    # Loop through markets and extract categories\n    for market in markets:\n        # Extract market slug and title\n        slug = market["slug"]\n        title = market["title"]\n        \n        # Check if slug or title contains keywords for manual categorization\n        if "Trump" in slug or "Biden" in slug or "Trump" in title or "Biden" in title:\n            category = "Politics"\n        elif "NBA" in slug or "EPL" in slug or "NBA" in title or "EPL" in title:\n            category = "Sports"\n        elif "Bitcoin" in slug or "ETH" in slug or "Bitcoin" in title or "ETH" in title:\n            category = "Crypto"\n        else:\n            category = "Other"\n        \n        # Add market to category dictionary\n        if category not in categories:\n            categories[category] = []\n        categories[category].append(market)\n    \n    # Print market categories\n    for category, markets in categories.items():\n        print(f"Category: {category}")\n        for market in markets:\n            print(f"* {market["title"]}")\n        print()\nelse:\n    print("Failed to retrieve markets")