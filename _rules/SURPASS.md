---
name: surpass
version: 3.7.0
severity: mandatory
scope: [competitive, planning, org-wide, strategy, implementation, research]
pairs-with: [engineering, verification, plan, anti-patterns, roles, mission, gate]
description: Org-wide agent OS — competitive research, strategic planning, implementation, and continuous surpass across every domain the organization operates in
---

# SURPASS.md
> **Single-file agent prompt. Drop this in any repo root.**  
> On first run: generates all working docs. On every run: reads them and continues.  
> Mission: Research → Analyze → Plan → Build → Verify → Surpass — across every product, team, and competitor.

---

## ⚡ HOW TO ACTIVATE

Paste this into your agent (Claude Code, Cursor, Aider, Copilot Workspace, etc.):

```
Read SURPASS.md in full, then execute the STARTUP SEQUENCE exactly as written.
```

That's it. The agent handles everything else.

---

## STARTUP SEQUENCE
> Run this checklist at the start of EVERY session — no exceptions. **Session** = any agent invocation that intends to do SURPASS work (research, implementation, or doc update). A new chat window, a new `task` subagent, or a CI run each count as a new session.

```
STEP 1 — GROUND IN THE REPO
  [ ] tree -L 3 (or equivalent) — read the full file structure
  [ ] Read README.md, CLAUDE.md, or any root-level spec
  [ ] Read package.json / pyproject.toml / go.mod / Cargo.toml — identify stack. If none exist, note stack is unknown and proceed.
  [ ] Answer internally: What does this platform DO? Who is the target user?

STEP 2 — CHECK FOR EXISTING DOCS (generated by a previous run)
  [ ] Does docs/research/FEATURE_MATRIX.md exist?
        YES → read it, load current competitor state
        NO  → it will be generated in this session (see §DOC-GEN)

  [ ] Does docs/research/GAP_ANALYSIS.md exist?
        YES → read it, identify highest-priority open gap
        NO  → it will be generated in this session (see §DOC-GEN)

  [ ] Does docs/exploration/CODEBASE_AUDIT.md exist?
        YES → read it. Compare its Stack and Directory Structure sections against the current repo state;
              annotate any differences as [CHANGED: date] in the new dated section you will append this session.
        NO  → run audit this session (see §3)

STEP 3 — ANNOUNCE PLAN
  Before executing anything, output:
  "SESSION PLAN: [what I will do this session and why, in 3-5 bullets]"
  Human-present mode: Wait for explicit approval ("go", "proceed", "ok") before executing.
  Autonomous mode: Output the plan, then proceed immediately without waiting.
  How to detect mode:
    Human-present = interactive TTY AND user sent the prompt manually (not via cron/CI/harness auto-accept).
    Autonomous    = any of: no TTY, running via cron/CI/background script, harness has auto-accept enabled,
                    or invoked by another agent (subagent/task delegation). When the invoker is itself an
                    agent, always treat as autonomous regardless of TTY state.
  When uncertain, default to HUMAN-PRESENT (safer — ask before acting).

STEP 4 — EXECUTE (follow §1 → §2 → §3 → §4 in order, starting where docs left off)
  If this is the first session and no docs exist, run §1 → §2 → §3 in full before §4.
  If docs exist, open docs/research/GAP_ANALYSIS.md and find the highest-priority open gap.
  Search for the exact string `- **Closed:** [ ]` (with the bold markers — `**Closed:**`, not plain `Closed:`).
  Priority order: P0 > P1 > P2. Tie-break within same priority: lowest GAP-XXX number first (oldest unresolved).
  If no open gaps remain, run §1 again to discover new competitors or re-evaluate the matrix.

STEP 5 — UPDATE DOCS AND DELIVER QA REPORT (mandatory before ending any session — see §DOC-GEN)
   Include all work completed this session; write "(none)" if no implementation occurred.
   If any feature was implemented this session, its QA report MUST be delivered inline to the user.
```


---

## Definitions & Disambiguation
  - **Major work block**: A focused unit of work that changes product behavior or architecture. Examples: adding a new feature, refactoring auth flow, running full competitor analysis. NOT a major work block: fixing a typo, updating a README, tweaking a CSS color, running `npm audit fix`.
  - **Confirm**: Verifiable by artifact. Examples of valid proof: terminal output showing test pass/fail, screenshot of UI behavior, git diff showing doc update, CI pipeline result. Examples of INVALID proof: "should work", "I think it's done", "tested mentally".
  - **Gap**: Anything classified ❌ or 🚧 affecting parity or surpass goals. Gaps must have GAP-XXX IDs. ID assignment: check `docs/research/GAP_ANALYSIS.md` for the highest existing ID and increment. If the file doesn't exist yet, start at GAP-001. Never reuse a closed gap's ID.
  - **Research**: Links, notes, and decisions backing implementation choices. Research MUST precede coding for P0/P1 work. Time-box: max 30 min per gap before starting implementation. If research is blocked (paywall, dead links, no data), document "RESEARCH BLOCKED — reason" and proceed with best available info.
  - **Feature**: A user-visible capability or measurable improvement. Avoid calling internal refactors "features" unless they change user-facing behavior or performance.
  - **Announce plan**: See STEP 3 above for the canonical definition. Do not interpret this differently elsewhere in the file.
  - **Standalone lines**: If a line looks like code or a title but is regular prose, treat it as Markdown text (do not assume template syntax).

---

## §MULTI-AGENT — Collaborative Research & Brainstorming Protocol
**→ Extracted to `MULTI_AGENT.md`.** Advocate + Skeptic + Synthesizer roles for high-quality decisions.
See [MULTI_AGENT.md](MULTI_AGENT.md) for full protocol — roles, execution modes, output format.

## §1 — COMPETITIVE INTELLIGENCE

### 1.1 Competitor Discovery

Identify all direct and indirect competitors. Minimum: 5. Target: 10+. If fewer than 5 exist (ultra-niche market), document why in FEATURE_MATRIX.md and proceed with however many are found — do not block on this number.

**Sources to check:** G2, ProductHunt, GitHub trending, HackerNews "Show HN", Reddit communities,
App Store / Play Store reviews, Crunchbase, Twitter/X search.

For each competitor, document using this exact template
(write to `docs/research/competitors/[name].md`):

```markdown
# Competitor: [Name]
- **URL / Repo:**
- **Category:** direct | indirect | emerging
- **Core Value Prop:**
- **Tech Stack (if known):**
- **Key Features (top 5):**
  1.
  2.
  3.
  4.
  5.
- **Known Weaknesses:**
- **Pricing Model:**
- **User Traction (estimate):**
- **Last Major Update:**
- **Unique Differentiator (their ⭐):**
- **What their users complain about:**
```

**[MULTI-AGENT] Competitor cross-check (run after all competitor files are written):**
- Advocate: "Review `docs/research/competitors/`. Make the case that we've found all significant competitors. What would we be missing if we stopped here?"
- Skeptic: "Challenge the competitor list. What direct/indirect competitors might be invisible to standard searches? Think: stealth startups, open-source alternatives, large platforms with adjacent features, regional competitors."
- Synthesizer: "Finalize the competitor list. Add any missed competitors. Mark confidence level (high/medium/low) per competitor based on data quality. Update FEATURE_MATRIX.md."

See §MULTI-AGENT for execution modes (parallel `task` agents or sequential solo simulation).

### 1.2 Feature Matrix

After competitor discovery, create or update `docs/research/FEATURE_MATRIX.md` using the canonical template in §DOC-GEN.
If the file already exists, add any new competitors as columns and fill in 🔍 for unknown cells.


**Status symbols:**
- ✅ Fully implemented — feature works end-to-end, tested, shipped
- 🚧 Partial / WIP — exists but incomplete, broken, or not production-ready
- ❌ Missing — feature does not exist in this product
- ⭐ Best-in-class — measurably better than every known competitor on ≥1 dimension
- 🔍 Not yet researched — use this as the initial state; replace with ✅/🚧/❌/⭐ once researched.
Every competitor's unique differentiator gets marked ⭐ in their column.
These automatically become P0 or P1 targets (see §1.3).

### 1.3 Gap Classification

After the matrix, classify every ❌ and 🚧 on our side:

- **P0** — Competitor has it, we don't. Blocker to being competitive. Fix first.
- **P1** — We have it but competitor does it better. Fix to surpass.
- **P2** — Nobody has it. First-mover opportunity. Reserve 20% capacity.

Write classified gaps to `docs/research/GAP_ANALYSIS.md`.
Create parent directories if missing.

**[MULTI-AGENT] Gap priority review (run after GAP_ANALYSIS.md is written):**
- Advocate: "Review the gap classifications. Defend the P0 list — why are these truly blockers? What evidence supports each priority?"
- Skeptic: "Challenge every P0. Are any actually P1 (nice-to-have parity, not a blocker)? Are any P2 gaps misclassified as P1? Is the effort estimate realistic?"
- Synthesizer: "Produce the final priority list. Resolve disagreements. If a gap's priority changed, update GAP_ANALYSIS.md and note the reason."

See §MULTI-AGENT for execution modes.

---

## §2 — DEEP RESEARCH

### 2.1 For Each P0/P1 Gap — Research Before Implementing

```markdown
# Research: [Feature / Gap Name]

## Industry Standard
[How do most platforms implement this?]

## State of the Art
[What's the best known implementation? Links to papers, repos, case studies.]

## Anti-patterns to Avoid
[What do implementations get wrong?]

## Options Evaluated
| Option | Pros | Cons | Complexity |
|--------|------|------|------------|
| A | | | S/M/L/XL |
| B | | | S/M/L/XL |
| C | | | S/M/L/XL |

Evaluate at least 3 options. If fewer exist (e.g., only one library does the job), document why alternatives were rejected rather than padding the table.

## Decision
[Selected option + rationale + trade-offs accepted]

## Reference Implementations
- [Link 1]
- [Link 2]
```

Create parent directories if missing.
Write to: `docs/decisions/[feature-name].md`

**Receipt requirement (per VERIFICATION.md §1):** Research is not "done" until the decision file exists on disk.
Proof = paste the first 5 lines of the written file after creation. "I researched it" without a file = NOT done.
If research is blocked (paywall, dead links, no data), write `docs/decisions/[feature-name].md` anyway with
section "## RESEARCH BLOCKED" and reason. A blocked research file is valid proof; a missing file is not.

**[MULTI-AGENT] Research debate (run before writing the Decision section):**
- Advocate: "Research [gap name]. Fill in Industry Standard, State of the Art, and propose Option A as the best choice. Write to `local://research-advocate-[gap].md`."
- Skeptic: "Read `local://research-advocate-[gap].md`. Challenge the chosen option: what are the hidden costs, integration risks, and maintenance burden? Propose Option B or C as a better alternative. Write to `local://research-skeptic-[gap].md`."
- Synthesizer: "Read both files. Resolve the debate. Fill in the final Options Evaluated table and write the Decision section. Output the complete `docs/decisions/[feature-name].md`."

Unresolved Skeptic objections = incomplete synthesis. List every objection and how it was resolved or consciously accepted.
See §MULTI-AGENT for execution modes and quality gates.

### 2.2 User Pain Point Mining

Before implementing any P0 or P1 feature (P2 is optional but recommended), check what users hate about competitor versions:
- Reddit: search `r/[industry]` + competitor name + "problem", "hate", "wish", "broken"
- App Store: filter 1–3 star reviews for competitors
- GitHub Issues: competitor repos, filter by "enhancement" + "frustration" labels

Add findings to the relevant `docs/research/competitors/[name].md` file.

Every user complaint = a design requirement for our version.

**[MULTI-AGENT] Brainstorm — "how should we solve this?" (run after pain points are collected):**
Use Explorer + Filter roles (see §MULTI-AGENT):
- Explorer: "Here are the user complaints about [competitor feature]: [paste list]. Generate 10+ ways we could solve these pains. Ignore feasibility — think first principles, analogies from other industries, and unconventional approaches."
- Filter: "Take the Explorer's list. Score each idea on: user impact (1-5), implementation effort (1-5), competitive differentiation (1-5). Keep top 3. Kill the rest with one sentence explaining why. Write output to `local://brainstorm-filter-[gap].md`."

The Filter output feeds directly into §2.1 Options Evaluated table. Do not skip this step for P0/P1 gaps.

---

## §3 — CODEBASE EXPLORATION

Run this at the start of every major work block (see **Definitions & Disambiguation** above for what counts), before implementation begins. If already run this session and no major structural changes occurred, skip re-run. **Major structural change = new directory added, dependency added/removed, module renamed/moved.** Quick check: `git diff HEAD~1 --stat`; if only logic/content files changed, skip is valid. Write findings to `docs/exploration/CODEBASE_AUDIT.md`. If the file exists, append a new dated section rather than overwriting.

**Required tool sequence:** Follow ENGINEERING.md §1 (mandatory read layer) — `list_projects` → `search_graph` → manual grep/read as fallback only.

```markdown
# Codebase Audit — [Date]

## Stack
[Languages, frameworks, key deps]

## Directory Structure
[Annotated tree — what lives where and why]

## Static Analysis
[Dead code, tech debt hotspots, code smells]

## Test Coverage
[Current coverage %, critical paths without tests]

## Performance
[Known bottlenecks, profiling results if available]

## Security
[Vulnerabilities, outdated deps, auth gaps]

## Architecture Score
| Dimension | Score | Notes |
|-----------|-------|-------|
| Scalability | 🔴/🟡/🟢 | |
| Maintainability | 🔴/🟡/🟢 | |
| Extensibility | 🔴/🟡/🟢 | |
| Observability | 🔴/🟡/🟢 | |
| Security | 🔴/🟡/🟢 | |

## Quick Wins (High impact, low effort — do now)
1.
2.

## Scheduled Improvements (High impact, high effort — roadmap)
1.
2.
```

---

## §4 — IMPLEMENTATION PROTOCOL
> **PLAN.md integration (mandatory for P0/P1):** Any gap classified COMPLEX (touches >2 files, >1 subsystem,
> or has unclear scope — "unclear scope" means: you cannot list all affected files before starting, the feature
> touches auth/billing/data-schema, or the gap description contains "refactor", "migrate", or "rearchitect".
> Note: SURPASS uses a stricter threshold than PLAN.md's >5 files because competitive gaps carry higher risk of
> scope creep) MUST go through **PLAN.md §2 decomposition** before any code is written.
> TRIVIAL gaps (single file, clear scope, <30 min, no schema/auth changes): 5-second mental check is sufficient.
> When uncertain about complexity: treat as COMPLEX.


### 4.1 Priority Order — Non-Negotiable

```
P0 FIRST. Always. No P1 work while any P0 is open.
  Exception: P0 status is 🚨 ESCALATION NEEDED (see FAILURE ESCALATION table) — in that case P1 work may proceed while awaiting human unblock.
P1 SECOND. Surpass, don't just match.
P2 THIRD. 20% capacity reserved. These become your moats.
```

### 4.2 Per-Feature Checklist
Before touching code for any P0 or P1 feature, confirm:

[ ] Gap is documented in GAP_ANALYSIS.md with a GAP-XXX ID
[ ] Research is written in docs/decisions/[feature].md
[ ] At least 3 implementation options were evaluated (or fewer documented with rationale)
[ ] Selected approach is documented with rationale
[ ] [MULTI-AGENT] Research debate completed (Advocate + Skeptic + Synthesizer outputs exist in local:// or docs/). Solo fallback: simulate roles sequentially — label each block [ADVOCATE] / [SKEPTIC] / [SYNTHESIZER]. Skipping the structure entirely is not valid.
[ ] [MULTI-AGENT] Brainstorm completed (Explorer + Filter outputs exist) — REQUIRED for P0/P1, optional for P2. Solo fallback: run Explorer then Filter sequentially in one session, labeled blocks. See §MULTI-AGENT for the full solo simulation protocol.
[ ] All Skeptic objections resolved or explicitly accepted in the decision file

P2 features: research is optional but recommended. If skipped, document why in the sprint notes (`docs/sprints/SPRINT_[N].md` — see §5 for sprint definition and numbering).

After implementation, confirm:

```
[ ] Passes ENGINEERING.md §2 DoD (tests written, no regression, inline docs, perf neutral or better)
[ ] QA scenarios generated (happy + sad flows — see §4.4), all executed, and QA report verdict is ALL PASS (not PARTIAL or FAIL)
[ ] QA report delivered to user
[ ] FEATURE_MATRIX.md updated (cell moved: ❌→✅, 🚧→✅, or any→⭐)
[ ] GAP_ANALYSIS.md updated (gap marked closed)
[ ] Passes "would a competitor's user switch for this?" test
```

### 4.3 The Surpass Standard

Do not stop at parity. For every feature:

| Level | Description | Status |
|-------|-------------|--------|
| ❌ Missing | Feature doesn't exist | Never acceptable |
| 🟡 Parity | Matches competitor | Temporary waypoint only |
| 🟢 Better | Measurably superior UX or perf | Minimum real target |
| ⭐ Best-in-class | No competitor matches it | Always aim here |

Ask before closing any feature: **"Is this ⭐, or did we stop at 🟡?"**

**What "⭐ Best-in-class" means concretely — pick at least one:**
- Speed/latency: measurably faster than every competitor benchmark you can find
- UX: user can complete the feature's primary use-case in fewer steps than any competitor (e.g., for auth: time-to-first-login; for search: query-to-result clicks)
- Coverage: handles an edge case or use-case no competitor documents
- Reliability: error rate or uptime metric provably better than public competitor SLAs
- DX: integration takes less code / fewer config steps than nearest alternative

If you cannot point to a specific, measurable dimension where the feature beats all known competitors, it is **not ⭐**. Downgrade to 🟢 and open a new gap.

### 4.4 QA Scenarios

After implementation and before marking a gap closed, generate QA scenarios. Every feature MUST have both happy path and sad path coverage. No exceptions.

#### What counts as a QA scenario

A QA scenario is a concrete, executable test case — not a vague description. Each scenario must specify: inputs, expected behavior, how to verify.

#### Happy path scenarios

Test the feature working as intended:

```markdown
## QA: [Feature Name] — Happy Path

### QA-H001: [Normal usage scenario]
- **Precondition:** [What must be true before this test]
- **Steps:**
  1. [Exact action]
  2. [Exact action]
  3. [Exact action]
- **Expected:** [Concrete observable outcome]
- **Actual:** [Fill during execution]
- **Status:** ✅ PASS / ❌ FAIL

### QA-H002: [Edge case that SHOULD work]
- **Precondition:**
- **Steps:**
- **Expected:**
- **Actual:**
- **Status:**
```

#### Sad path scenarios

Test failure modes, invalid inputs, and error handling:

```markdown
## QA: [Feature Name] — Sad Path

### QA-S001: [Invalid input scenario]
- **Precondition:** [What must be true before this test]
- **Steps:**
  1. [Provide invalid/missing input]
  2. [Trigger the action]
- **Expected:** [Clear error message / graceful degradation / no crash]
- **Actual:** [Fill during execution]
- **Status:** ✅ PASS / ❌ FAIL

### QA-S002: [Boundary / empty / null scenario]
- **Precondition:**
- **Steps:**
- **Expected:**
- **Actual:**
- **Status:**

### QA-S003: [Permission / auth failure scenario]
- **Precondition:**
- **Steps:**
- **Expected:**
- **Actual:**
- **Status:**
```

#### Minimum coverage rules

| Feature type | Min happy | Min sad | Extra required |
|-------------|-----------|---------|----------------|
| CRUD / API | 3 | 3 | Auth failure, invalid body, idempotency |
| UI / Form | 2 | 3 | Empty fields, invalid format, back/refresh |
| Background job | 2 | 2 | Retry, timeout, partial failure |
| Integration | 2 | 3 | API down, timeout, malformed response |
| Data migration | 2 | 2 | Rollback, partial migration, duplicate data |

#### QA report delivery

After executing all scenarios, deliver a QA report to the user. The report MUST be:
1. Printed inline in the conversation (not hidden in a file)
2. Written to `docs/qa/QA_REPORT_[feature-name].md`

The report format (see §DOC-GEN for canonical template):

```markdown
# QA Report: [Feature Name]
> Date: [Date] | Gap: [GAP-XXX] | Verdict: PASS / FAIL / PARTIAL

## Summary
- Total scenarios: [N]
- Passed: [N]
- Failed: [N]
- Blocked: [N]

## Happy Path Results
| ID | Scenario | Status | Notes |
|----|----------|--------|-------|
| QA-H001 | [name] | ✅/❌ | |
| QA-H002 | [name] | ✅/❌ | |

## Sad Path Results
| ID | Scenario | Status | Notes |
|----|----------|--------|-------|
| QA-S001 | [name] | ✅/❌ | |
| QA-S002 | [name] | ✅/❌ | |

## Failures (if any)
| ID | What happened | Expected | Severity | Fix needed |
|----|--------------|----------|----------|-----------|

## Verdict
☐ ALL PASS — gap can be closed
☐ PARTIAL — list blockers before closing
☐ FAIL — do not close the gap, fix failures first
```

**Hard rule:** A gap cannot be marked closed in GAP_ANALYSIS.md until its QA report verdict is ALL PASS. PARTIAL or FAIL means the gap stays open.

Create parent directories if missing.


---

## §5 — SPRINT LOOP

**Sprint trigger** (one of these must be true to write a sprint doc):
- ≥3 PRs merged this session, OR
- Any single PR that closes a P0 gap

After a sprint trigger fires, write to `docs/sprints/SPRINT_[N].md` where N is an incrementing integer starting at 1. If unsure of the next number, check `docs/sprints/` for the highest existing `[N]` and increment. If the directory is empty or does not exist, start at N=1 and create the directory.

**Repo-less / no-git runs:** If working in an environment without git (no `.git` dir), treat "sprint" as a
time-bounded session: write a sprint doc after every session where ≥1 gap was closed or ≥3 research files
were written. Use wall-clock date as the sprint ID (e.g., `SPRINT_2026-07-05.md`). Do not block on git.

```markdown
# Sprint [N] — [Date Range]

## What We Shipped
-

## Feature Matrix Delta
| Feature | Before | After |
|---------|--------|-------|
| [e.g. Dark mode] | ❌ | ✅ |
| [e.g. Search speed] | ✅ | ⭐ |
| [e.g. Export to CSV] | 🚧 | ✅ |

## New Gaps Discovered
-

## "Switch test" — what would make a competitor user switch to us RIGHT NOW?
-

## Next Sprint Priority (top 3)
1.
2.
3.
```

---

## §DOC-GEN — DOCUMENT GENERATION SPEC
**→ Extracted to `DOC_TEMPLATES.md`.** Canonical templates for all doc types: feature matrices, gap analysis, competitor files, decision logs, QA reports, sprint notes.
See [DOC_TEMPLATES.md](DOC_TEMPLATES.md) for the full spec.

---

## §6 — SUCCESS CRITERIA
> The mission is complete when ALL of these are true.

```
[ ] FEATURE_MATRIX.md: zero ❌ in our column vs. top 3 competitors
[ ] Performance: p95 latency ≤ best competitor on equivalent hardware
[ ] UX: blind user test shows preference for us over competitors.
    Definition of "blind": testers do not know which product is ours. Minimum 3 testers.
    Fallback (no human testers available): agent-run task-completion audit —
      1. Take competitor screenshots of the same core flow.
      2. Count steps-to-completion for competitor vs. ours.
      3. Document results in docs/qa/.
      4. ✅ only if our step count ≤ competitor's AND no dead-ends observed.
    A verbal claim "our UX is better" with no audit file = ❌ not met.
[ ] Reliability: uptime SLA ≥ industry standard. Definition: "industry standard" = 99.9% for SaaS (three nines). If a competitor publishes a higher SLA, use that as the bar instead. Proof: link to status page or published SLA doc.
[ ] Time-to-value: ≤ fastest competitor's onboarding. Definition: "time-to-value" = wall-clock minutes from account creation to completing the product's primary use-case for the first time. Measure both ours and the fastest competitor's. Document results in docs/qa/.
[ ] Moats: ≥ 3 features where we are ⭐ with no competitor match
[ ] Traction: active user/revenue metric trending up vs. competitors
```

Until every box is checked — the loop continues.

---

## HARD RULES

| Rule | No exception |
|------|-------------|
| Read SURPASS.md fully at the start of every session — before touching docs or code (you are reading it now; this rule means do not skip re-reading it in future sessions) | Yes |
| Read existing docs before writing new code | Yes |
| No code without a documented gap to close | Yes |
| No P1 work while P0 gaps are open — unless P0 is formally escalated (status 🚨 ESCALATION NEEDED) | Yes — except when P0 is blocked and escalated |
| No feature marked done without docs updated | Yes |
| Parity is a waypoint, not the destination | Yes |
| Always verify competitor state — never assume | Yes |

---

## FAILURE ESCALATION
> When you cannot close a gap this session, do not leave it silently open. Pick the row below that matches your situation.

| Situation | Action |
|-----------|--------|
| Blocked by missing data / access | Document "BLOCKED: reason" in GAP_ANALYSIS.md, set status 🚧, tag `blocker:data` |
| Blocked by upstream dependency | Split gap into two: unblocked sub-task + blocked parent. Work the sub-task. |
| P0 gap, no path forward | STOP, output STATUS REPORT with "🚨 ESCALATION NEEDED" header, list exactly what human must unblock |
| Research exhausted, still uncertain | Mark gap as `confidence:low`, implement best-available option, note assumptions explicitly in code |
| Time budget exceeded | Primary: >2h wall-clock on one gap. Secondary (only if harness shows token count): >50k tokens on one gap. Action: ship partial work, create follow-up gap GAP-XXX-b with the same priority as the parent gap (inherit P0→P0, P1→P1, P2→P2), note in STATUS REPORT. Do NOT close the parent gap — mark it 🚧. |
| Same fix approach tried 2+ times with no progress (anti-thrash, AP-012) | STOP. Do not try a third variation. Re-read the gap definition — is the problem statement correct? Try a fundamentally different approach or escalate. Document what was tried and why it failed in GAP_ANALYSIS.md. |
---

## STATUS REPORT FORMAT
> Use this whenever reporting progress to a human.

```markdown
## Status Report — [Date]

**Session goal:** [1 line]

**Docs read this session:**
- [ ] SURPASS.md
- [ ] FEATURE_MATRIX.md
- [ ] GAP_ANALYSIS.md
- [ ] CODEBASE_AUDIT.md

**Research findings:**
-
-

**Implemented:**
- [Feature] → closes [GAP-XXX]

**Matrix updates:**
| Feature | Before | After |
|---------|--------|-------|

**Docs updated:**
-

**Next priority:** [GAP-XXX — name]

**Blockers for human:**
-
```

---

> 💡 *"Parity is not victory. Victory is when a competitor's user tries us and never goes back."*  
> 💡 *"Every ❌ in the matrix is a user we're losing right now."*  
> ⚠️ *If you're unsure what to work on — open GAP_ANALYSIS.md, find the top open P0, and close it.*

---

## SESSION END SIGNAL
Every session MUST close with one of these terminal outputs — no exceptions. Print this block inline in the conversation (not hidden in a file). If ending BLOCKED or PARTIAL, also paste it at the top of the STATUS REPORT below.

**Receipt requirement (per VERIFICATION.md §1):** Before printing this block, confirm each item is verifiable:
- "Gaps closed" → each listed GAP-ID must have `Closed: [x]` in GAP_ANALYSIS.md on disk.
- "Docs updated" → each listed file must have been written or appended during this invocation. Concrete check: read the first 5 lines of the file's newest section and confirm they contain content written this session. Do not rely on OS mtime alone (unreliable in CI/autonomous mode). Alternative when file read is unavailable: use a write-hash — hash the content you wrote and store it in the STATUS REPORT; re-read the file and confirm the hash matches.
- A session ending COMPLETE with zero verifiable receipts is invalid — downgrade to PARTIAL.

```
SURPASS SESSION END
Status: [COMPLETE | BLOCKED | PARTIAL]
Gaps closed this session: [list GAP-IDs or "none"]
Gaps opened this session: [list GAP-IDs or "none"]
Next priority: [GAP-XXX — name, or "none open"]
Handoff note: [1-2 sentences for the next agent/human picking this up]
```

If ending BLOCKED or PARTIAL, also output a STATUS REPORT (see above).

---

## OPEN-SOURCE REFERENCES
These repos encode hard-won agent OS knowledge. Read them when improving this framework.

| Repo | What to borrow |
|------|---------------|
| [anthropics/anthropic-cookbook](https://github.com/anthropics/anthropic-cookbook) | Prompt patterns, tool-use examples, context window management |
| [x1xhlol/system-prompts-and-models-of-ai-tools](https://github.com/x1xhlol/system-prompts-and-models-of-ai-tools) | Real system prompts from Cursor, Devin, v0 — study their startup sequences |
| [agentsmd/agents.md](https://github.com/agentsmd/agents.md) | AGENTS.md standard — canonical format for agent context files |
| [multica-ai/andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills) | Karpathy's 4 principles: think-before-coding, simplicity, surgical changes, goal-driven |
| [businessascode/company-os](https://businessascode.ai) | Company-as-code pattern: structured knowledge for people + AI agents |
| [run-llama/llama_index](https://github.com/run-llama/llama_index) | RAG pipeline patterns for grounding research steps |
| [BerriAI/litellm](https://github.com/BerriAI/litellm) | Multi-provider LLM routing — useful when competitor analysis requires diverse model calls |

**How to use these references:**
- When updating SURPASS.md, check if any of the above have published improvements since your knowledge cutoff.
- When stuck on a pattern (gap analysis, QA format, research depth), read the relevant repo's README/docs first.
- Do NOT blindly copy — adapt to the 1ai-rules context and document what you borrowed and why in the commit message.
