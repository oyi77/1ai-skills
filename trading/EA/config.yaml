# Maybe HFT EA Configuration
# Save as config.yaml dan jalankan: python maybe_hft.py --config config.yaml

# Trading Parameters
symbol: "GC=X"
lots: 0.10
stoploss: 1500
trailing: 500
trail_start: 1000
x_distance: 300
slippage: 30
magic: 12345
start_direction: 0  # 0=BUY dulu, 1=SELL dulu

# Broker Settings
broker: "auto"  # mt5, simulated, auto
mode: "paper"   # paper, live

# Run Settings
once: false
interval: 1.0
